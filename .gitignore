# Dependencies
node_modules/

# Your entries database: every selection, note and window title you have captured.
server/*.db
server/*.db-wal
server/*.db-shm

# The JSON Lines log from before the database. No longer read.
server/captures.jsonl

# Built web app: npm run build in web/
web/dist/

# Built from the .gschema.xml by install.sh
extension/schemas/gschemas.compiled

*.log
